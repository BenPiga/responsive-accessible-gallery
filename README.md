# responsive-accessible-gallery
A mobile-first, responsive web gallery demonstrating advanced CSS media queries. Features adaptive layouts using CSS Grid, custom dark mode styling (prefers-color-scheme), and reduced motion accessibility (prefers-reduced-motion).
